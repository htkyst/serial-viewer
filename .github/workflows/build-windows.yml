name: Build Windows

on:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: windows-latest

        steps:
            - name : Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                node-version: '24'
                cache: npm

            - name: Install fronted dependencies
              run: npm ci 

            - name: Setup Rust
              uses: dtolnay/rust-toolchain@stable
            
            - name: Build
              run: npm run tauri build

            - name: Create ZIP
              shell: pwsh
              run: |
                mkdir package
                copy src-tauri\target\release\serial-viewer.exe package\serial-viewer.exe
                Compress-Archive -Path package\* -DestinationPath serial-viewer.zip
            
            - name: Upload ZIP
              uses: actions/upload-artifact@v4
              with:
                name: serial-viewer-windows-x86_64
                path: serial-viewer-windows-x86_64.zip